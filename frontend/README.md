# CreatorIQ Frontend

React + Vite.

## Setup

```bash
cd frontend
npm install
```

## Run

```bash
npm run dev
```

App runs at http://localhost:5173 — make sure the backend is running at
http://localhost:8000 first (see backend/README.md).
